// @flow

export type IconProps = {
    name: string,
    title?: string,
    className?: string,
    color: GlobalColorType,
    size: IconSizeType,
    onClick?: Function,
    refHandler?: Function
}
