// @flow

export type Props = {
    className?: string,
    children: React$Node,
    size?: FontSizeType,
    color?: GlobalColorType,
    weight?: number,
    align?: string,
    ellipsis?: boolean,
    noSelect?: boolean
}
