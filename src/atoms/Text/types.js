// @flow

export type Props = {
    children: React$Node,
    size?: FontSizeType,
    color?: GlobalColorType,
    weight?: number,
    align?: string,
    ellipsis?: boolean,
    noSelect?: boolean
}
