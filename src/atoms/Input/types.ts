// @flow

export type Props = {
    onChange: Function,
    value: string,
    placeholder?: string,
    size?: SizeTypes,
    refHandler?: Function,
    disabled?: boolean,
    isFlat?: boolean,
    isFit?: boolean
}
