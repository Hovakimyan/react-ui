// @flow

export type Props = {
    onChange: Function,
    value: string,
    size?: SizeTypes,
    refHandler?: Function,
    className?: string,
    disabled?: boolean,
    isFlat?: boolean,
    isFit?: boolean
}
